# NL-027

Source ad 1059406850353566, French, Valérie.L / Avenor Paris. Target nl-NL.

## What changed on the image

Nothing. The creative is a photograph with no text on it, so it was uploaded as-is. `after.png` is the same picture as `before.jpg`.

## What changed in the copy

All three fields were transcreated into Dutch. The primary text is a story of about 1,000 words, so it was rewritten to read as Dutch rather than translated line by line. Line breaks, both emoji, every name and the avenorparis.com address are kept.

The headline lands on 40 characters and the description on 125. Both sit exactly on their limit, so any later edit needs a re-count.

Copy judge: fidelity 5, fluency 5. Status Finished.

## Files

- `source.json`: the French fields as fetched from the Ad Library
- `target.json`: the Dutch fields, the translator notes and the judge result
- `before.jpg`: the creative as published
- `after.png`: the file written to Drive
