# NL-028

Source ad 27369494302749728, French, Valérie.L / Avenor Paris. Target nl-NL.

## What changed on the image

This is a designed creative: a banner, a large offer line, a sub-line and four icon captions. Seven strings were replaced.

| Before | After |
| --- | --- |
| AUJOURD'HUI SEULEMENT | ALLEEN VANDAAG |
| 1 ACHETÉ = 1 OFFERT | 1 GEKOCHT = 1 GRATIS |
| VOTRE TEINTE. INSTANTANÉMENT. | UW TINT. DIRECT. |
| TEINTE SUR MESURE | TINT OP MAAT |
| FORMULE HYDRATANTE | HYDRATERENDE FORMULE |
| DU TRANSPARENT À VOTRE TEINTE | VAN TRANSPARANT NAAR UW TINT |
| TENUE LONGUE DURÉE | LANGHOUDEND |

The typefaces, the colours, the four icons, the product shot and the background are unchanged. The output was read back independently and all seven Dutch strings were confirmed present.

## Why the status is Needs Review

The copy could not be sourced. The Meta Ad Library lists this ad as having multiple versions and shows no primary text, no headline and no description for it. There was nothing to translate, so the three copy fields are empty.

The image work is finished and the file is in Drive. The row is flagged so a person decides what copy goes with it. The system reporting an empty source rather than inventing three fields is the behaviour we want.

## Files

- `source.json`: the fields as fetched. The three copy fields are empty, which is what the Ad Library returned.
- `target.json`: the seven image strings and a note recording why no copy exists
- `before.jpg`: the creative as published
- `after.png`: the file written to Drive
